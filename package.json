{
  "name": "adarkroom-gleam",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run i18n && gleam build && vite",
    "build": "npm run i18n && gleam build && vite build",
    "i18n": "node scripts/po2json.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
